[project]
name = "lab-azure-fullstack-de25-johnny-hyytiainen"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv.workspace]
members = [
    "backend",
    "frontend",
]

[dependency-groups]
dev = [
    "ipykernel>=7.3.0",
    "pandas>=3.0.5",
    "plotly>=7.0.0",
    "pyarrow>=25.0.1",
    "pytest>=9.1.1",
    "ruff>=0.16.5",
]
